from __future__ import annotations

from fastapi.responses import HTMLResponse


DASHBOARD_HTML = r'''<!doctype html>
<html lang="vi">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>NPD AI Command Center</title>
<style>
:root{font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#172033;background:#f3f6fb;--blue:#155eef;--navy:#101828;--border:#e4e7ec;--muted:#667085;--surface:#fff;--green:#067647;--red:#b42318;--amber:#b54708}*{box-sizing:border-box}body{margin:0}.top{background:linear-gradient(120deg,#101828,#1d2939);color:#fff;padding:17px max(20px,calc((100vw - 1400px)/2));display:flex;gap:14px;align-items:center;justify-content:space-between;flex-wrap:wrap;position:sticky;top:0;z-index:20;box-shadow:0 6px 18px rgba(16,24,40,.16)}.brand{display:flex;align-items:center;gap:10px}.brand-mark{display:grid;place-items:center;width:34px;height:34px;border-radius:10px;background:#155eef;font-weight:800}.top h1{font-size:19px;margin:0}.top-sub{font-size:12px;color:#d0d5dd}.auth{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.auth input,.auth select,.auth button,textarea,button{font:inherit}.auth input{width:min(420px,70vw);padding:9px 11px;border:1px solid #475467;border-radius:8px;background:#fff;color:#111827}.auth button,.auth a,.primary{padding:9px 13px;border:0;border-radius:8px;background:var(--blue);color:#fff;cursor:pointer;text-decoration:none}.wrap{max-width:1400px;margin:auto;padding:20px}.notice{padding:12px 14px;border-radius:10px;background:var(--surface);border:1px solid var(--border);margin-bottom:16px}.source-list{display:flex;gap:6px;flex-wrap:wrap}.source{font-size:12px;padding:4px 8px;border-radius:999px;background:#f2f4f7;color:#475467}.source.available,.source.configured,.source.read_only{background:#ecfdf3;color:var(--green)}.source.failed,.source.not_configured{background:#fef3f2;color:var(--red)}.source.contract_only,.source.partial{background:#fffaeb;color:var(--amber)}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:12px}.card{background:var(--surface);border:1px solid var(--border);border-radius:13px;padding:15px;box-shadow:0 1px 2px rgba(16,24,40,.04)}.metric{font-size:28px;font-weight:760;margin-top:5px}.muted{color:var(--muted);font-size:13px}.section{margin-top:22px}.section-head{display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;margin-bottom:9px}.section h2,.section-head h2{font-size:17px;margin:0}.section-note{font-size:12px;color:var(--muted)}.composer{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(300px,.85fr);gap:14px}.composer textarea{width:100%;min-height:126px;padding:13px;border:1px solid #d0d5dd;border-radius:10px;resize:vertical;line-height:1.5}.composer textarea:focus{outline:3px solid #dbeafe;border-color:var(--blue)}.agent-guide{background:#f8faff;border:1px solid #dbe7ff;border-radius:11px;padding:13px}.agent-guide strong{display:block;margin-bottom:7px}.agent-guide ul{margin:7px 0 0;padding-left:18px;color:#475467;font-size:13px;line-height:1.55}.row{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.question-library{margin-top:12px;background:var(--surface);border:1px solid var(--border);border-radius:13px;overflow:hidden}.question-library summary{cursor:pointer;padding:14px 16px;font-weight:700;list-style:none;display:flex;justify-content:space-between;align-items:center}.question-library summary::-webkit-details-marker{display:none}.question-library summary span{font-size:12px;font-weight:500;color:var(--muted)}.question-filters{display:flex;gap:7px;flex-wrap:wrap;padding:0 16px 12px}.filterbtn{border:1px solid #d0d5dd;background:#fff;color:#344054;border-radius:999px;padding:6px 10px;cursor:pointer;font-size:12px}.filterbtn.active{background:#eff4ff;border-color:#84adff;color:#175cd3}.questions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;padding:0 16px 16px}.question{display:flex;gap:10px;text-align:left;align-items:flex-start;padding:11px;border:1px solid var(--border);border-radius:10px;background:#fff;cursor:pointer;color:#344054;line-height:1.4}.question:hover{border-color:#84adff;background:#f8faff}.qnum{display:grid;place-items:center;min-width:24px;height:24px;border-radius:7px;background:#eff4ff;color:#175cd3;font-weight:700;font-size:12px}.qagent{display:block;color:var(--muted);font-size:11px;margin-top:4px}.panel{background:var(--surface);border:1px solid var(--border);border-radius:12px;overflow:auto}table{width:100%;border-collapse:collapse;min-width:760px}th,td{text-align:left;padding:11px;border-bottom:1px solid #edf0f4;font-size:13px;vertical-align:top}th{background:#f8fafc;color:#475467;font-weight:650;position:sticky;top:0}tr:last-child td{border-bottom:0}.tag{display:inline-block;padding:3px 7px;border-radius:999px;background:#eef2ff;margin:2px;font-size:12px}.danger{background:var(--red)!important}.secondary{background:#475467!important}.smallbtn{padding:6px 9px;border:0;border-radius:7px;background:var(--blue);color:#fff;cursor:pointer;font-size:12px}.smallbtn.secondary{background:#667085}.smallbtn.danger{background:var(--red)}.error{color:var(--red)}.ok{color:var(--green)}.answer{border-left:4px solid var(--blue)}.answer h3{margin:0 0 8px}.answer ul{padding-left:20px}.answer-item{margin-top:10px;padding:12px;border-radius:9px;background:#f8fafc}.answer-item.high{border-left:4px solid var(--red)}.answer-item.medium{border-left:4px solid #f79009}.answer-item.low{border-left:4px solid #12b76a}.answer-metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(165px,1fr));gap:8px;margin:12px 0}.answer-metric{padding:10px;background:#f5f8ff;border:1px solid #e0eaff;border-radius:9px;font-size:12px}.answer-metric strong{display:block;color:#475467;margin-bottom:3px}.answer-metric .value{font-size:16px;font-weight:750;color:#101828}.campaign-wrap{margin-top:14px}.campaign-wrap h4{margin:0 0 8px}.campaign-table{min-width:1120px}.campaign-table th:nth-child(3),.campaign-table td:nth-child(3){text-align:right;font-weight:700;white-space:nowrap}.campaign-table td:nth-child(n+4):nth-child(-n+9){text-align:right;white-space:nowrap}.priority{display:inline-block;padding:3px 7px;border-radius:999px;font-size:11px;background:#f2f4f7}.priority.high{background:#fef3f2;color:var(--red)}.priority.medium{background:#fffaeb;color:var(--amber)}.priority.low{background:#ecfdf3;color:var(--green)}.approval-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:10px}.approval{border-top:3px solid #f79009}.approval p{font-size:13px;line-height:1.45}.approval-meta{display:flex;justify-content:space-between;gap:8px;margin-bottom:8px}.approval-plan{margin-top:12px;padding:12px;border:1px solid #f2d39b;border-radius:10px;background:#fffcf5}.approval-plan h4{margin:0 0 7px;font-size:13px}.approval-plan ol,.approval-plan ul{margin:7px 0;padding-left:19px;font-size:13px;line-height:1.5}.approval-facts{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:7px;margin-top:9px}.approval-fact{padding:8px 9px;border:1px solid #f1e0bb;border-radius:8px;background:#fff}.approval-fact span{display:block;color:var(--muted);font-size:11px;margin-bottom:3px}.approval-fact strong{font-size:12px;overflow-wrap:anywhere}.approval-reason{margin:9px 0 0}.approval-warning{margin-top:9px;padding:9px;border-radius:8px;background:#fff4e5;color:#7a2e0e;font-size:12px;line-height:1.45}.approval-scope{margin-top:9px;font-size:12px;color:#475467}.task-objective{max-width:500px;line-height:1.4}.task-id{font-size:11px;color:var(--muted)}.empty{grid-column:1/-1}.audit-details summary{cursor:pointer;font-weight:700;padding:3px 0}.campaign-os{border-top:4px solid #7f56d9}.campaign-create{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:9px}.campaign-create textarea{min-height:78px;width:100%;padding:11px;border:1px solid #d0d5dd;border-radius:9px;resize:vertical}.campaign-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:9px;margin-top:12px}.campaign-card{padding:12px;border:1px solid var(--border);border-radius:10px;background:#fff;cursor:pointer}.campaign-card:hover{border-color:#84adff}.campaign-tabs{display:flex;gap:6px;overflow:auto;margin:14px 0 9px;padding-bottom:3px}.campaign-tab{white-space:nowrap;border:1px solid #d0d5dd;background:#fff;padding:7px 10px;border-radius:8px;cursor:pointer;font-size:12px}.campaign-tab.active{background:#f4f3ff;border-color:#9e77ed;color:#6941c6}.campaign-detail-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:8px}.campaign-data{padding:10px;border:1px solid var(--border);border-radius:9px;background:#f8fafc;font-size:13px;line-height:1.45}.campaign-data strong{display:block;margin-bottom:4px}.campaign-json{white-space:pre-wrap;overflow-wrap:anywhere;font-size:12px;background:#101828;color:#f2f4f7;padding:12px;border-radius:9px;max-height:420px;overflow:auto}@media(max-width:900px){.composer{grid-template-columns:1fr}.questions{grid-template-columns:1fr}.campaign-create{grid-template-columns:1fr}}@media(max-width:760px){.approval-grid{grid-template-columns:1fr}}@media(max-width:600px){.wrap{padding:12px}.top{padding:13px}.auth input{width:100%}.metric{font-size:24px}.answer-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.questions{padding:0 10px 10px}.question-filters{padding:0 10px 10px}.question-library summary{padding:12px}.section{margin-top:18px}.approval-facts{grid-template-columns:1fr}.campaign-detail-grid{grid-template-columns:1fr}}
.attribution-os{border-top:4px solid #0e9384}
.attribution-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:9px}
.attribution-state{padding:11px;border:1px solid #b2ddff;border-radius:9px;background:#f5fbff}
.attribution-state strong{display:block;margin-bottom:4px}
.experiment-os{border-top:4px solid #7a5af8}
.experiment-form{display:grid;grid-template-columns:minmax(220px,.7fr) minmax(320px,1.3fr) 130px 130px auto;gap:8px;align-items:end}
.experiment-form label{font-size:12px;color:var(--muted)}
.experiment-form input,.experiment-form select,.experiment-form textarea{display:block;width:100%;margin-top:4px;padding:9px;border:1px solid #d0d5dd;border-radius:8px;background:#fff}
.experiment-form textarea{min-height:62px;resize:vertical}
.experiment-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:9px;margin-top:12px}
@media(max-width:600px){.attribution-grid{grid-template-columns:1fr}}
@media(max-width:1000px){.experiment-form{grid-template-columns:1fr 1fr}.experiment-form label:nth-child(2){grid-column:1/-1}}@media(max-width:600px){.experiment-form{grid-template-columns:1fr}.experiment-form label:nth-child(2){grid-column:auto}}
</style>
</head>
<body>
<div class="top"><div class="brand"><div class="brand-mark">AI</div><div><h1>NPD AI Command Center</h1><div class="top-sub">Campaign OS · Attribution & Revenue · CRM · Marketing · Content</div></div></div><div class="auth"><input id="token" type="password" autocomplete="off" placeholder="Bearer token (owner/operator/viewer)"/><button onclick="saveToken()">Kết nối</button></div></div>
<div class="wrap">
<div id="status" class="notice">Nhập token để tải dữ liệu. Token chỉ lưu trong sessionStorage của trình duyệt.</div>
<div class="grid">
<div class="card"><div class="muted">Task đang theo dõi</div><div id="mTasks" class="metric">–</div></div>
<div class="card"><div class="muted">Approval cần owner</div><div id="mPending" class="metric">–</div></div>
<div class="card"><div class="muted">Action đã thực thi</div><div id="mExecuted" class="metric">–</div></div>
<div class="card"><div class="muted">Execution thất bại</div><div id="mFailed" class="metric">–</div></div>
</div>

<div class="section campaign-os">
  <div class="section-head"><div><h2>Campaign Workspace</h2><span class="section-note">Research → plan → draft → preview. Production execution luôn tắt trong Phase 6B.</span></div><button class="smallbtn secondary" onclick="loadCampaigns()">Làm mới</button></div>
  <div class="card">
    <div class="campaign-create"><textarea id="campaignBrief">Tạo chiến dịch Vịnh Tiên tháng 9, ngân sách 100 triệu, mục tiêu 300 lead và 30 khách đi xem.</textarea><button class="primary" onclick="createCampaignBrief()">Tạo campaign plan</button></div>
    <div id="campaignProviders" class="source-list" style="margin-top:9px"></div>
    <div id="campaignList" class="campaign-list"><div class="muted">Chưa tải Campaign OS.</div></div>
    <div id="campaignDetail"></div>
  </div>
</div>

<div class="section attribution-os">
  <div class="section-head"><div><h2>Attribution & Revenue OS</h2><span class="section-note">Shadow chỉ-đọc. Doanh thu bị khóa cho đến khi owner chấp thuận quality gate.</span></div><div class="row"><button class="smallbtn secondary" onclick="loadAttributionOpportunitySource()">Kiểm tra Opportunity</button><button class="smallbtn secondary" onclick="loadAttributionStatus()">Làm mới</button></div></div>
  <div class="card">
    <div id="attributionStatus" class="attribution-grid"><div class="muted">Chưa tải trạng thái attribution.</div></div>
    <div class="section-head" style="margin-top:14px"><div><strong>Campaign Identity & Data Quality</strong><div class="section-note">Chỉ map bằng ID đã xác minh hoặc UTM contract; không suy đoán dự án từ tên Ads.</div></div><button class="smallbtn secondary" onclick="loadAttributionIdentity()">Kiểm tra mapping</button></div>
    <div id="attributionIdentity" class="attribution-grid"><div class="muted">Chưa tải identity coverage.</div></div>
    <div id="attributionIdentityMappings" class="campaign-detail-grid" style="margin-top:9px"></div>
    <div id="attributionIdentityIssues" class="notice" style="margin:10px 0 0">Chưa có snapshot ingest pseudonymous.</div>
    <div class="section-head" style="margin-top:14px"><div><strong>Lead Intake exception queue</strong><div class="section-note">Giữ unknown/conflict ở shadow store; chỉ replay sau khi ID được owner xác minh.</div></div><button class="smallbtn secondary" onclick="loadAttributionIntakeIssues()">Làm mới queue</button></div>
    <div id="attributionIntakeIssues" class="campaign-detail-grid"><div class="muted">Chưa tải Lead Intake exceptions.</div></div>
    <div id="attributionIntakePreview"></div>
    <div id="attributionSource" class="notice" style="margin:10px 0 0">Nguồn EspoCRM Opportunity chỉ đọc khi owner/operator yêu cầu; UI không hiển thị ID bản ghi.</div>
    <div class="notice" style="margin:10px 0 0">Không ghi CRM, không đổi Ads và không liên hệ khách hàng. CAC/ROAS không được suy diễn nếu chưa có chi phí cùng Campaign/kỳ đã đối soát.</div>
  </div>
</div>

<div class="section experiment-os">
  <div class="section-head"><div><h2>Experiment & Optimization OS</h2><span class="section-note">Plan → preview → đọc GA4/Meta → owner xác nhận dữ liệu → khuyến nghị. Không phân bổ traffic hoặc đổi production.</span></div><button class="smallbtn secondary" onclick="loadExperiments()">Làm mới</button></div>
  <div class="card">
    <div id="experimentStatus" class="attribution-grid"><div class="muted">Chưa tải trạng thái Experiment OS.</div></div>
    <div class="experiment-form" style="margin-top:12px">
      <label>Campaign<select id="experimentCampaign"><option value="">Chọn Campaign</option></select></label>
      <label>Giả thuyết<textarea id="experimentHypothesis">Hook nêu lợi ích đầu tư rõ hơn sẽ tăng tỷ lệ khách gửi form.</textarea></label>
      <label>Baseline (%)<input id="experimentBaseline" type="number" min="0" step="0.01" value="2.5" /></label>
      <label>Target lift (%)<input id="experimentLift" type="number" min="0.01" step="0.1" value="20" /></label>
      <button class="primary" onclick="createExperimentPlan()">Tạo A/B plan</button>
    </div>
    <div class="notice" style="margin:10px 0 0"><strong>Đánh giá bằng dữ liệu chỉ-đọc.</strong> GA4 dùng campaign ID/utm_content; Meta chỉ đọc khi Campaign và từng ad ID được map tường minh. Owner phải chấp nhận snapshot trước evaluation. Khuyến nghị không tự chạy experiment.</div>
    <div id="experimentList" class="experiment-list"><div class="muted">Chưa có experiment.</div></div>
    <div id="experimentDetail"></div>
  </div>
</div>

<div class="section">
  <div class="section-head"><h2>Giao việc cho AI Commander</h2><span class="section-note">Mô tả mục tiêu, khoảng thời gian và kết quả cần so sánh.</span></div>
  <div class="card composer">
    <div>
      <textarea id="objective" placeholder="Ví dụ: So sánh các chiến dịch Meta Ads của Bat Dong San 1 và Bat Dong San 4 trong 30 ngày theo số tiền đã chi, CTR, CPC, CPL và lead Meta; đề xuất việc cần ưu tiên nhưng không tự thay đổi ngân sách."></textarea>
      <div class="row" style="margin-top:9px"><button class="primary" onclick="createTask()">Phân tích bằng AI Agents</button><button class="smallbtn secondary" onclick="clearObjective()">Xóa</button><span class="muted">Cần role operator hoặc owner.</span></div>
    </div>
    <div class="agent-guide"><strong>Cách giao việc để có câu trả lời sát nhất</strong><ul><li>Nêu rõ kỳ phân tích: 7, 30 hoặc 90 ngày.</li><li>Chỉ định tiêu chí: chi phí, CPL, lead, chuyển đổi hoặc SLA.</li><li>Yêu cầu so sánh và đề xuất hành động; mọi thao tác ghi vẫn cần owner duyệt.</li></ul><div id="sources" class="source-list" style="margin-top:10px"></div></div>
  </div>
</div>

<details class="question-library" open>
  <summary>20 câu hỏi nghiệp vụ gợi ý <span>Nhấn một câu để đưa vào ô giao việc — không tự chạy</span></summary>
  <div id="questionFilters" class="question-filters"></div>
  <div id="questions" class="questions"></div>
</details>

<div class="section"><div class="section-head"><h2>Kết quả trả lời</h2><span class="section-note">Dữ liệu tổng hợp chỉ-đọc; các giới hạn được ghi rõ trong từng kết quả.</span></div><div id="answer" class="notice">Chọn một câu hỏi nghiệp vụ, hoặc mở kết quả của task gần đây.</div></div>

<div class="section"><div class="section-head"><h2>Approval queue</h2><span class="section-note">Ưu tiên xử lý trước danh sách task. Chỉ owner có quyền quyết định.</span></div><div id="approvals" class="approval-grid"></div></div>

<div class="section"><div class="section-head"><div><h2>5 task gần nhất</h2><span class="section-note">Danh sách được rút gọn để tập trung vào công việc mới nhất.</span></div><button class="smallbtn secondary" onclick="refreshAll()">Làm mới</button></div><div class="panel"><table><thead><tr><th>Cập nhật</th><th>Mục tiêu</th><th>AI Agents</th><th>Actions</th><th>Approval</th><th>Failed</th><th>Kết quả</th></tr></thead><tbody id="tasks"></tbody></table></div></div>

<div class="section"><details class="audit-details"><summary>Audit gần đây</summary><div class="panel" style="margin-top:10px"><table><thead><tr><th>Thời gian</th><th>Event</th><th>Task</th><th>Action</th><th>Agent</th></tr></thead><tbody id="audit"></tbody></table></div></details></div>
</div>
<script>
const BUSINESS_QUESTIONS=[
{group:'CRM & Sales',agent:'CRM Manager + Sales',text:'Lead nào đang active quá 24 giờ chưa được cập nhật? Xếp thứ tự ưu tiên chăm sóc hôm nay và nêu lý do.'},
{group:'CRM & Sales',agent:'CRM Manager + Sales',text:'Chọn 5 lead cần ưu tiên follow-up hôm nay dựa trên trạng thái, mức độ quan tâm và lần cập nhật gần nhất.'},
{group:'CRM & Sales',agent:'CRM Manager',text:'Phân tích pipeline CRM theo trạng thái trong 30 ngày: số lead, tỷ trọng và điểm nghẽn cần xử lý.'},
{group:'CRM & Sales',agent:'CRM Manager + Marketing',text:'So sánh các nguồn lead theo số lượng và tỷ lệ Converted; nguồn nào cần ưu tiên kiểm tra chất lượng?'},
{group:'CRM & Sales',agent:'Sales',text:'Có bao nhiêu lead thiếu kênh liên hệ hoặc chưa được giao phụ trách? Đề xuất cách xử lý theo thứ tự ưu tiên.'},
{group:'Marketing',agent:'Marketing Leader',text:'So sánh các chiến dịch Meta Ads của Bat Dong San 1 và Bat Dong San 4 trong 30 ngày theo số tiền đã chi, impressions, clicks, CTR, CPC, CPL và lead Meta.'},
{group:'Marketing',agent:'Marketing Leader',text:'Chiến dịch Meta Ads nào chi nhiều nhưng chưa tạo lead trong 30 ngày? Lập bảng và nêu việc cần kiểm tra.'},
{group:'Marketing',agent:'Marketing Leader',text:'Chiến dịch nào có CPL tốt nhất trong 30 ngày? Đề xuất ưu tiên ngân sách nhưng không tự thay đổi ngân sách.'},
{group:'Marketing',agent:'Marketing Leader',text:'Báo cáo hiệu quả marketing đa nguồn 30 ngày từ Meta Ads, GA4, Social và CRM; chỉ rõ dữ liệu nào chưa đối soát.'},
{group:'Marketing',agent:'Marketing Leader + CRM Manager',text:'So sánh clicks Ads, website sessions và lead CRM trong 30 ngày để tìm điểm rơi rụng của funnel.'},
{group:'Website & GA4',agent:'Marketing Leader',text:'Báo cáo GA4 30 ngày: sessions, users, key events và các kênh mang traffic chính.'},
{group:'Website & GA4',agent:'Marketing Leader',text:'Báo cáo hiệu quả website 7 ngày gần nhất theo sessions, users, key events và nêu tín hiệu cần theo dõi.'},
{group:'Website & GA4',agent:'Marketing Leader + CRM Manager',text:'Kênh website nào tạo nhiều lượt truy cập nhưng chưa phản ánh thành lead CRM? Nêu giới hạn attribution.'},
{group:'Social & Content',agent:'Social Media',text:'Báo cáo hiệu quả Facebook Page Ngọc Phương Đông trong 30 ngày theo followers, reactions, comments, shares và engagements.'},
{group:'Social & Content',agent:'Content Trend + Social Media',text:'Lập kế hoạch nội dung Facebook 7 ngày dựa trên mục tiêu tăng tương tác, ghi rõ giả định khi thiếu dữ liệu nội dung bài.'},
{group:'Social & Content',agent:'Video Producer + Content Trend',text:'Đề xuất 5 ý tưởng video ngắn bất động sản cho tuần tới, kèm mục tiêu, hook và CTA; chưa xuất bản.'},
{group:'Điều hành',agent:'Tất cả AI Agents',text:'Cho tôi tổng quan toàn bộ hệ thống và 3 ưu tiên điều hành cần xử lý hôm nay.'},
{group:'Điều hành',agent:'Tất cả AI Agents',text:'Phối hợp các AI Agent lập kế hoạch hành động 7 ngày cho Marketing, Sales, CRM, Content, Video và Social.'},
{group:'Điều hành',agent:'Marketing Leader + CRM Manager + Sales',text:'Đánh giá tổng hợp hiệu quả marketing và chăm sóc lead trong 30 ngày; chỉ ra 3 điểm nghẽn cần owner ưu tiên.'},
{group:'Điều hành',agent:'Tất cả AI Agents',text:'Quản lý công việc ngày mai: lập checklist ưu tiên cho Marketing, Sales, CRM, Content, Video và Social; không tự thực hiện thao tác ghi.'}
];
const $=id=>document.getElementById(id);let token=sessionStorage.getItem('npd_agent_token')||'';$('token').value=token;let activeQuestionGroup='Tất cả';
function setStatus(text,kind=''){const e=$('status');e.textContent=text;e.className='notice '+kind}
function saveToken(){token=$('token').value.trim();sessionStorage.setItem('npd_agent_token',token);refreshAll()}
async function api(path,options={}){const headers=Object.assign({},options.headers||{});if(token)headers.Authorization='Bearer '+token;if(options.body)headers['Content-Type']='application/json';const r=await fetch(path,Object.assign({},options,{headers}));if(!r.ok){let d;try{d=await r.json()}catch{d={detail:r.statusText}};throw new Error((d&&d.detail)||('HTTP '+r.status))}return r.status===204?null:r.json()}
function esc(v){return String(v??'').replace(/[&<>"']/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[s]))}
function number(v,digits=0){const n=Number(v);return Number.isFinite(n)?new Intl.NumberFormat('vi-VN',{maximumFractionDigits:digits}).format(n):'—'}
function money(v,currency='VND'){const n=Number(v);return Number.isFinite(n)?new Intl.NumberFormat('vi-VN',{style:'currency',currency,maximumFractionDigits:0}).format(n):'—'}
function dateTime(v){const d=new Date(v);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('vi-VN',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'}).format(d)}
function clearObjective(){$('objective').value='';$('objective').focus()}
function renderQuestions(){const groups=['Tất cả',...new Set(BUSINESS_QUESTIONS.map(q=>q.group))];$('questionFilters').innerHTML=groups.map(g=>`<button class="filterbtn ${g===activeQuestionGroup?'active':''}" onclick="filterQuestions('${esc(g)}')">${esc(g)}</button>`).join('');$('questions').innerHTML=BUSINESS_QUESTIONS.map((q,i)=>({q,i})).filter(x=>activeQuestionGroup==='Tất cả'||x.q.group===activeQuestionGroup).map(x=>`<button class="question" onclick="useQuestion(${x.i})"><span class="qnum">${x.i+1}</span><span>${esc(x.q.text)}<span class="qagent">${esc(x.q.agent)} · ${esc(x.q.group)}</span></span></button>`).join('')}
function filterQuestions(group){activeQuestionGroup=group;renderQuestions()}
function useQuestion(index){$('objective').value=BUSINESS_QUESTIONS[index].text;$('objective').focus();$('objective').scrollIntoView({behavior:'smooth',block:'center'})}
async function refreshAll(){try{const [me,s,sources,providers,attribution,identity,experiments]=await Promise.all([api('/api/v1/whoami'),api('/api/v1/command-center?limit=20&audit_limit=20'),api('/api/v1/integrations/marketing/status'),api('/api/v1/integrations/campaign/status'),api('/api/v1/attribution/status'),api('/api/v1/attribution/identity/status'),api('/api/v1/experiments/status')]);setStatus('Đã xác thực: '+me.role+' · storage: '+s.storage_backend,'ok');render(s);renderSources(sources);renderCampaignProviders(providers);renderAttributionStatus(attribution);renderAttributionIdentityStatus(identity);renderExperimentStatus(experiments);await loadCampaigns();await loadExperiments();await loadAttributionIdentityMappings();await loadAttributionIntakeIssues()}catch(e){setStatus(e.message,'error')}}
function renderSources(sources){$('sources').innerHTML=Object.entries(sources||{}).map(([name,status])=>`<span class="source ${esc(status)}">${esc(name)}: ${esc(status)}</span>`).join('')}
function renderCampaignProviders(providers){$('campaignProviders').innerHTML=Object.entries(providers||{}).map(([name,p])=>`<span class="source ${esc(p.status)}">${esc(name)}: ${esc(p.status)}</span>`).join('')}
let latestReconciliationId='',latestAttributionState='not_started';
function renderAttributionStatus(s){latestReconciliationId=s.latest_reconciliation_id||'';latestAttributionState=s.latest_state||'not_started';$('attributionStatus').innerHTML=`<div class="attribution-state"><strong>Chế độ</strong>${esc(s.mode)}</div><div class="attribution-state"><strong>Touchpoints bất biến</strong>${number(s.touchpoint_count)}</div><div class="attribution-state"><strong>Reconciliation snapshots</strong>${number(s.reconciliation_count)}</div><div class="attribution-state"><strong>Quality gate gần nhất</strong>${esc(s.latest_state)}</div><div class="attribution-state"><strong>Production write</strong>${s.production_write_enabled?'enabled':'disabled'}</div>`}
function renderAttributionIdentityStatus(s){const q=s.latest_snapshot||{};$('attributionIdentity').innerHTML=`<div class="attribution-state"><strong>Verified mappings</strong>${number(s.mapping_count)}</div><div class="attribution-state"><strong>Pending intake</strong>${number(s.pending_intake_issues||0)}</div><div class="attribution-state"><strong>Identity coverage</strong>${q.coverage_rate===undefined?'chưa có':number(q.coverage_rate*100,1)+'%'}</div><div class="attribution-state"><strong>Unknown / conflict</strong>${number(q.unknown||0)} / ${number(q.conflicts||0)}</div><div class="attribution-state"><strong>Freshness</strong>${esc(q.freshness_state||'no_data')}${q.freshness_age_hours===null||q.freshness_age_hours===undefined?'':' · '+number(q.freshness_age_hours,1)+' giờ'}</div><div class="attribution-state"><strong>Production write</strong>${s.production_write_enabled?'enabled':'disabled'}</div>`;const issues=q.issues||[];$('attributionIdentityIssues').innerHTML=issues.length?`<strong>Cảnh báo mapping / mismatch</strong><div class="muted" style="margin-top:5px">${issues.map(x=>`${esc(x.state)} · ${esc(x.detail)}${(x.candidate_campaign_ids||[]).length?' · '+x.candidate_campaign_ids.map(esc).join(', '):''}`).join('<br>')}</div>`:'<strong>Không có mismatch trong snapshot gần nhất.</strong><div class="muted">Unknown/conflict không được ghi vào immutable ledger.</div>'}
async function loadAttributionIdentityMappings(){const rows=await api('/api/v1/attribution/identity-mappings?limit=50');$('attributionIdentityMappings').innerHTML=rows.length?rows.map(x=>`<div class="campaign-data"><strong>${esc(x.campaign_id)} · ${esc(x.project)}</strong>${esc(x.source_system)} · campaign ${esc(x.source_campaign_id||'—')} · ad ${esc(x.source_ad_id||'—')}<div class="muted">${esc(x.verification)} · ${dateTime(x.created_at)} · Write: disabled</div></div>`).join(''):'<div class="campaign-data"><strong>Chưa có verified mapping</strong>Source event sẽ ở trạng thái unknown; hệ thống không suy đoán bằng tên.</div>'}
async function loadAttributionIdentity(){try{const [s]=await Promise.all([api('/api/v1/attribution/identity/status'),loadAttributionIdentityMappings()]);renderAttributionIdentityStatus(s)}catch(e){setStatus(e.message,'error')}}
async function loadAttributionIntakeIssues(){try{const rows=await api('/api/v1/attribution/intake/issues?status=pending&limit=50');$('attributionIntakeIssues').innerHTML=rows.length?rows.map(x=>`<div class="campaign-data"><strong>${esc(x.state)} · ${esc(x.source_event.source_system)}</strong><span class="task-id">${esc(x.issue_id)}</span><div class="muted">Event ${esc(x.source_event.source_event_id)} · Campaign ${esc(x.source_event.source_campaign_id||'—')} · Ad ${esc(x.source_event.source_ad_id||'—')} · gặp ${number(x.occurrence_count)} lần</div><div>${esc(x.detail)}</div><div class="row" style="margin-top:8px"><button class="smallbtn secondary" onclick="previewAttributionIntakeIssue('${esc(x.issue_id)}')">Preview</button><button class="smallbtn" onclick="replayAttributionIntakeIssue('${esc(x.issue_id)}')">Replay shadow</button></div></div>`).join(''):'<div class="campaign-data"><strong>Queue sạch</strong>Không có unknown/conflict đang chờ xử lý.</div>'}catch(e){setStatus(e.message,'error')}}
async function previewAttributionIntakeIssue(id){try{const p=await api('/api/v1/attribution/intake/issues/'+encodeURIComponent(id)+'/preview');$('attributionIntakePreview').innerHTML=`<div class="notice" style="margin-top:10px"><strong>${esc(p.state)} · ${esc(p.issue_id)}</strong><div>${esc(p.detail)}</div><div class="muted">Campaign: ${(p.candidate_campaign_ids||[]).map(esc).join(', ')||'chưa xác định'} · Ledger: ${esc(p.ledger_event_id)} · Would insert: ${p.would_insert?'có':'không'} · External write: disabled</div></div>`}catch(e){setStatus(e.message,'error')}}
async function replayAttributionIntakeIssue(id){if(!confirm('REPLAY SHADOW?\n\nChỉ ghi vào immutable attribution ledger sau verified mapping; không sửa Ads, CRM hoặc liên hệ khách.'))return;try{const s=await api('/api/v1/attribution/intake/issues/'+encodeURIComponent(id)+'/replay',{method:'POST'});setStatus('Replay hoàn tất: inserted '+s.inserted+', duplicate '+s.duplicates+'. Không có external write.','ok');await loadAttributionIdentity();await loadAttributionIntakeIssues();$('attributionIntakePreview').innerHTML=''}catch(e){setStatus(e.message,'error')}}
async function loadAttributionStatus(){try{const [s]=await Promise.all([api('/api/v1/attribution/status'),loadAttributionIdentity()]);renderAttributionStatus(s)}catch(e){setStatus(e.message,'error')}}
async function loadAttributionOpportunitySource(){try{const s=await api('/api/v1/attribution/sources/espocrm/opportunities?limit=200');$('attributionSource').innerHTML=`<strong>EspoCRM Opportunity: ${esc(s.status)}</strong><div class="muted">Tổng CRM: ${number(s.reported_total)} · Đã đọc: ${number(s.records_read)} · Campaign field: ${esc(s.campaign_field)} · Raw PII: ${s.contains_raw_pii?'có':'không'} · Write: ${s.external_writes_enabled?'enabled':'disabled'}</div><div class="muted">Projection: ${(s.projection||[]).map(esc).join(' · ')}</div>`}catch(e){setStatus(e.message,'error')}}
let selectedCampaign=null,activeCampaignTab='overview';
const CAMPAIGN_TABS=[['overview','Campaign Overview'],['kpis','KPIs'],['channels','Channel Plans'],['creatives','Creatives'],['landing','Landing Pages'],['email','Email'],['zalo','Zalo/ZBS'],['tracking','Tracking'],['approvals','Approvals'],['funnel','Lead Funnel']];
async function loadCampaigns(){const rows=await api('/api/v1/campaigns?limit=20');$('campaignList').innerHTML=rows.length?rows.map(c=>`<button class="campaign-card" onclick="openCampaign('${esc(c.campaign_id)}')"><strong>${esc(c.name)}</strong><div class="task-id">${esc(c.campaign_id)}</div><div class="row" style="margin-top:7px"><span class="tag">${esc(c.status)}</span><span class="muted">${money(c.budget.amount,c.budget.currency)}</span></div></button>`).join(''):'<div class="muted">Chưa có campaign. Dùng brief mẫu để tạo plan.</div>';const select=$('experimentCampaign');const selected=select.value;select.innerHTML='<option value="">Chọn Campaign</option>'+rows.map(c=>`<option value="${esc(c.campaign_id)}">${esc(c.name)} · ${esc(c.campaign_id)}</option>`).join('');if(rows.some(c=>c.campaign_id===selected))select.value=selected;if(selectedCampaign){const current=rows.find(c=>c.campaign_id===selectedCampaign.campaign_id);if(current){selectedCampaign=current;renderCampaignDetail()}}}
async function createCampaignBrief(){const request=$('campaignBrief').value.trim();if(request.length<10)return setStatus('Campaign brief quá ngắn','error');try{setStatus('Đang tạo Campaign OS plan/preview…');selectedCampaign=await api('/api/v1/campaigns/from-brief',{method:'POST',body:JSON.stringify({request})});activeCampaignTab='overview';renderCampaignDetail();await loadCampaigns();setStatus('Đã tạo '+selectedCampaign.campaign_id+'; chưa execute live.','ok')}catch(e){setStatus(e.message,'error')}}
async function openCampaign(id){selectedCampaign=await api('/api/v1/campaigns/'+encodeURIComponent(id));activeCampaignTab='overview';renderCampaignDetail();$('campaignDetail').scrollIntoView({behavior:'smooth',block:'start'})}
function campaignPanel(c){if(activeCampaignTab==='overview')return `<div class="campaign-detail-grid"><div class="campaign-data"><strong>Mục tiêu</strong>${esc(c.objective)}</div><div class="campaign-data"><strong>Ngân sách</strong>${money(c.budget.amount,c.budget.currency)}</div><div class="campaign-data"><strong>Thời gian</strong>${esc(c.start_date)} → ${esc(c.end_date)}</div><div class="campaign-data"><strong>Owner</strong>${esc(c.audit_metadata.owner)}</div></div>`;if(activeCampaignTab==='kpis')return `<div class="campaign-detail-grid">${(c.kpi_targets||[]).map(k=>`<div class="campaign-data"><strong>${esc(k.name)}</strong>${number(k.target,2)} ${esc(k.unit)} · ${esc(k.funnel_stage)}</div>`).join('')}</div>`;if(activeCampaignTab==='channels')return `<div class="campaign-detail-grid">${(c.channel_plans||[]).map(p=>`<div class="campaign-data"><strong>${esc(p.channel)} · ${esc(p.specialist_agent)}</strong><span class="source ${esc(p.provider_status)}">${esc(p.provider_status)}</span><div>${esc(p.objective)}</div><div class="muted">${(p.deliverables||[]).map(esc).join(' · ')}</div><div><strong>Execution: ${p.execution_enabled?'enabled':'disabled'}</strong></div></div>`).join('')}</div>`;if(activeCampaignTab==='creatives')return `<div class="campaign-detail-grid">${(c.creatives||[]).map(x=>`<div class="campaign-data"><strong>${esc(x.name)}</strong>${esc(x.hook)}<div class="muted">${esc(x.format)} · CTA: ${esc(x.cta)}</div></div>`).join('')}</div>`;if(activeCampaignTab==='landing')return `<div class="campaign-detail-grid">${(c.landing_pages||[]).map(x=>`<div class="campaign-data"><strong>${esc(x.title)}</strong>${esc(x.environment)} · ${esc(x.target_system)}<div>${esc(x.target_path)}</div><div class="muted">Production publish: disabled</div></div>`).join('')}</div>`;if(activeCampaignTab==='email')return `<pre class="campaign-json">${esc(JSON.stringify(c.email_sequence_refs||[],null,2))}</pre>`;if(activeCampaignTab==='zalo')return `<pre class="campaign-json">${esc(JSON.stringify(c.zalo_zbs_sequence_refs||[],null,2))}</pre>`;if(activeCampaignTab==='tracking')return `<pre class="campaign-json">${esc(JSON.stringify(c.tracking||{},null,2))}</pre>`;if(activeCampaignTab==='approvals')return `<div class="campaign-detail-grid">${(c.approval_package||[]).map(a=>`<div class="campaign-data"><strong>${esc(a.scope)} · ${esc(a.target_system)}</strong>${esc(a.action)}<div class="muted">Owner approval: required · Execution: disabled</div></div>`).join('')}</div><div class="row" style="margin-top:9px"><button class="smallbtn" onclick="requestCampaignApproval()">Gửi owner duyệt</button>${c.status==='awaiting_approval'?'<button class="smallbtn" onclick="campaignDecision(true)">Owner phê duyệt package</button><button class="smallbtn danger" onclick="campaignDecision(false)">Từ chối</button>':''}</div>`;return `<div class="campaign-detail-grid"><div class="campaign-data"><strong>Lead target</strong>${esc((c.kpi_targets||[]).find(x=>x.funnel_stage==='lead')?.target||'—')}</div><div class="campaign-data"><strong>Site-visit target</strong>${esc((c.kpi_targets||[]).find(x=>x.funnel_stage==='site_visit')?.target||'—')}</div><div class="campaign-data"><strong>CRM tracking</strong>EspoCRM/Sales Hub read-only placeholder</div><div class="campaign-data"><strong>Sales SLA</strong>${esc(c.sales_handoff.first_response_sla_minutes)} phút phản hồi đầu · ${esc(c.sales_handoff.visit_booking_sla_hours)} giờ booking</div></div>`}
function renderCampaignDetail(){const c=selectedCampaign;if(!c){$('campaignDetail').innerHTML='';return}$('campaignDetail').innerHTML=`<div class="campaign-tabs">${CAMPAIGN_TABS.map(([id,label])=>`<button class="campaign-tab ${id===activeCampaignTab?'active':''}" onclick="setCampaignTab('${id}')">${esc(label)}</button>`).join('')}</div><div class="row" style="justify-content:space-between;margin-bottom:9px"><div><strong>${esc(c.name)}</strong><div class="task-id">${esc(c.campaign_id)}</div></div><span class="tag">${esc(c.status)}</span></div>${campaignPanel(c)}`}
function setCampaignTab(tab){activeCampaignTab=tab;renderCampaignDetail()}
async function requestCampaignApproval(){selectedCampaign=await api('/api/v1/campaigns/'+encodeURIComponent(selectedCampaign.campaign_id)+'/approvals/request',{method:'POST',body:JSON.stringify({scope:'campaign',note:'Campaign OS planning package ready for owner review'})});activeCampaignTab='approvals';renderCampaignDetail();await loadCampaigns()}
async function campaignDecision(approved){if(!confirm((approved?'PHÊ DUYỆT':'TỪ CHỐI')+' campaign planning package?\n\nKhông action nào được execute live trong Phase 6B.'))return;selectedCampaign=await api('/api/v1/campaigns/'+encodeURIComponent(selectedCampaign.campaign_id)+'/approvals/campaign/decision',{method:'POST',body:JSON.stringify({approved,note:'Owner decision from Campaign Workspace'})});renderCampaignDetail();await loadCampaigns()}
function renderExperimentStatus(s){const sources=Object.entries(s.observation_sources||{}).map(([name,status])=>`<span class="source ${esc(status)}">${esc(name)}: ${esc(status)}</span>`).join('');$('experimentStatus').innerHTML=`<div class="attribution-state"><strong>Chế độ</strong>${esc(s.mode)}</div><div class="attribution-state"><strong>Experiment</strong>${number(s.experiment_count)}</div><div class="attribution-state"><strong>GA4 tracking sẵn sàng</strong>${number(s.ga4_tracking_ready)}</div><div class="attribution-state"><strong>Meta tracking sẵn sàng</strong>${number(s.meta_tracking_ready)}</div><div class="attribution-state"><strong>Observations</strong>${number(s.observation_count)}</div><div class="attribution-state"><strong>Chờ owner xác nhận</strong>${number(s.observations_pending_owner)}</div><div class="attribution-state"><strong>Đã xác nhận chất lượng</strong>${number(s.observations_quality_accepted)}</div><div class="attribution-state"><strong>Đã đánh giá</strong>${number(s.evaluated)}</div><div class="attribution-state"><strong>Chờ dữ liệu</strong>${number(s.awaiting_observation)}</div><div class="attribution-state"><strong>Production execution</strong>${s.production_execution_enabled?'enabled':'disabled'}</div><div class="attribution-state" style="grid-column:1/-1"><strong>Nguồn variant</strong>${sources||'<span class="muted">Chưa cấu hình</span>'}</div>`}
async function loadExperiments(){const [s,rows]=await Promise.all([api('/api/v1/experiments/status'),api('/api/v1/experiments?limit=20')]);renderExperimentStatus(s);$('experimentList').innerHTML=rows.length?rows.map(x=>{const e=x.last_evaluation;return `<div class="campaign-card"><strong>${esc(x.name)}</strong><div class="task-id">${esc(x.experiment_id)} · ${esc(x.campaign_id)}</div><div class="row" style="margin-top:7px"><span class="tag">${esc(x.status)}</span><span class="muted">${esc(x.experiment_type)} · lift ${number(x.target_lift_percent,2)}%</span></div><div class="muted" style="margin-top:7px">Observations: ${number((x.observations||[]).length)} · Khuyến nghị: ${esc(e?.recommendation||'chưa có')}</div><div class="row" style="margin-top:9px"><button class="smallbtn" onclick="previewExperiment('${esc(x.experiment_id)}')">Preview</button><button class="smallbtn secondary" onclick="inspectExperimentEvidence('${esc(x.experiment_id)}')">Dữ liệu & đánh giá</button><button class="smallbtn secondary" onclick="requestExperimentApproval('${esc(x.experiment_id)}')">Gửi owner duyệt</button>${x.status==='awaiting_approval'?`<button class="smallbtn" onclick="decideExperiment('${esc(x.experiment_id)}',true)">Duyệt plan</button>`:''}</div></div>`}).join(''):'<div class="muted">Chưa có experiment plan.</div>'}
async function createExperimentPlan(){const campaignId=$('experimentCampaign').value;const hypothesis=$('experimentHypothesis').value.trim();const baseline=Number($('experimentBaseline').value);const lift=Number($('experimentLift').value);if(!campaignId)return setStatus('Hãy chọn Campaign','error');if(latestAttributionState!=='quality_accepted'||!latestReconciliationId)return setStatus('Cần attribution snapshot đã được owner chấp thuận','error');if(hypothesis.length<10)return setStatus('Giả thuyết quá ngắn','error');const payload={campaign_id:campaignId,attribution_reconciliation_id:latestReconciliationId,name:'A/B creative hook',experiment_type:'creative',hypothesis,primary_metric:{name:'Form conversion rate',unit:'percent',direction:'increase',source:'GA4 accepted campaign events'},baseline_value:baseline,target_lift_percent:lift,variants:[{variant_id:'VAR-CONTROL',name:'Control',description:'Creative hiện tại giữ nguyên.',allocation_percent:50},{variant_id:'VAR-HOOKA',name:'Benefit hook',description:'Creative draft với hook lợi ích đầu tư.',allocation_percent:50}],guardrails:[{metric:'Cost per qualified lead',operator:'<=',threshold:1500000,unit:'VND',action:'stop_and_review'}],stop_conditions:[{condition:'Guardrail breached for two review windows',reason:'Protect lead economics',automatic_execution:false}],evaluation_window_days:14,owner:'nguyenvanvangct@gmail.com'};try{const x=await api('/api/v1/experiments',{method:'POST',body:JSON.stringify(payload)});setStatus('Đã tạo '+x.experiment_id+' ở plan-only; chưa chạy traffic.','ok');await loadExperiments()}catch(e){setStatus(e.message,'error')}}
async function previewExperiment(id){try{const p=await api('/api/v1/experiments/'+encodeURIComponent(id)+'/preview',{method:'POST'});$('experimentDetail').innerHTML=`<div class="notice" style="margin-top:12px"><strong>${esc(p.experiment_id)} · Preview only</strong><div class="muted">Primary metric: ${esc(p.primary_metric.name)} · baseline ${number(p.baseline_value,2)} → target ${number(p.target_value,2)} ${esc(p.primary_metric.unit)} · ${number(p.evaluation_window_days)} ngày</div><div class="campaign-detail-grid" style="margin-top:9px">${p.variants.map(v=>`<div class="campaign-data"><strong>${esc(v.name)} · ${number(v.allocation_percent)}%</strong>${esc(v.description)}</div>`).join('')}</div><div class="muted" style="margin-top:8px">External writes: disabled · Traffic allocation: chưa thực thi</div></div>`;await loadExperiments()}catch(e){setStatus(e.message,'error')}}
async function requestExperimentApproval(id){try{await api('/api/v1/experiments/'+encodeURIComponent(id)+'/approvals/request',{method:'POST',body:JSON.stringify({note:'Experiment plan/preview ready for owner review; execution remains disabled.'})});await loadExperiments()}catch(e){setStatus(e.message,'error')}}
async function decideExperiment(id,approved){if(!confirm((approved?'PHÊ DUYỆT':'TỪ CHỐI')+' experiment plan?\n\nQuyết định này không khởi chạy traffic hoặc thay đổi production.'))return;try{await api('/api/v1/experiments/'+encodeURIComponent(id)+'/approvals/decision',{method:'POST',body:JSON.stringify({approved,note:'Owner decision from Experiment Workspace; plan only.'})});await loadExperiments()}catch(e){setStatus(e.message,'error')}}
async function inspectExperimentEvidence(id){try{const root='/api/v1/experiments/'+encodeURIComponent(id);const [x,observations,ga4,meta]=await Promise.all([api(root),api(root+'/observations?limit=10'),api(root+'/tracking-validation?source_system=ga4'),api(root+'/tracking-validation?source_system=meta_ads')]);const e=x.last_evaluation;const rows=observations.map(o=>`<div class="campaign-data"><strong>${esc(o.source_system)} · ${esc(o.source_state)} · quality ${esc(o.quality_state)}</strong>${esc(o.window_start)} → ${esc(o.window_end)}<div class="muted">Sample: ${(o.variants||[]).map(v=>`${esc(v.variant_id)} ${number(v.sample_size)} / conversion ${number(v.conversions)}`).join(' · ')} · Raw PII: không · External write: disabled</div>${o.quality_state==='pending_owner'?`<div class="row" style="margin-top:8px"><button class="smallbtn" onclick="decideObservationQuality('${esc(id)}','${esc(o.observation_id)}',true)">Owner chấp nhận dữ liệu</button><button class="smallbtn danger" onclick="decideObservationQuality('${esc(id)}','${esc(o.observation_id)}',false)">Từ chối</button></div>`:''}</div>`).join('');const accepted=observations.find(o=>o.quality_state==='accepted');const variantArg=encodeURIComponent(JSON.stringify((x.variants||[]).map(v=>v.variant_id)));const urlRows=Object.entries(ga4.tracked_urls||{}).map(([variant,url])=>`<div class="campaign-data"><strong>${esc(variant)} · utm_content</strong><span class="task-id">${esc(url)}</span></div>`).join('');$('experimentDetail').innerHTML=`<div class="notice" style="margin-top:12px"><strong>${esc(id)} · Tracking activation & read-only evidence</strong><div class="campaign-detail-grid" style="margin-top:9px"><div class="campaign-data"><strong>GA4 tracking: ${esc(ga4.state)}</strong>${(ga4.issues||[]).map(esc).join(' · ')||'Campaign ID và UTM variant đã sẵn sàng.'}</div><div class="campaign-data"><strong>Meta tracking: ${esc(meta.state)}</strong>${(meta.issues||[]).map(esc).join(' · ')||'Campaign/ad IDs đã được owner map.'}</div></div><div class="campaign-detail-grid" style="margin-top:9px">${urlRows}</div><div class="row" style="margin-top:9px"><button class="smallbtn secondary" onclick="readExperimentSource('${esc(id)}','ga4')">Đọc GA4 14 ngày</button><button class="smallbtn secondary" onclick="readExperimentSource('${esc(id)}','meta_ads')">Đọc Meta 14 ngày</button><button class="smallbtn" onclick="applyMetaMapping('${esc(id)}','${variantArg}')" ${observations.length?'disabled':''}>Owner khai báo Meta IDs</button></div><div class="campaign-detail-grid" style="margin-top:9px">${rows||'<div class="campaign-data"><strong>Chưa có observation</strong>Kiểm tra tracking rồi đọc trực tiếp nguồn aggregate; hệ thống không tạo dữ liệu giả.</div>'}</div>${e?`<div class="approval-plan"><h4>Khuyến nghị: ${esc(e.recommendation)}</h4><div>Sample đủ: ${e.sample_sufficient?'có':'không'} · Nguồn mới: ${e.source_fresh?'có':'không'} · Lift: ${number(e.observed_lift_percent,2)}% · p-value: ${number(e.p_value,4)}</div><div class="muted">${(e.reasons||[]).map(esc).join(' · ')}</div><div class="approval-warning">Advisory only · Không tự đổi traffic, ngân sách hoặc production.</div></div>`:'<div class="muted" style="margin-top:8px">Chưa có evaluation.</div>'}<div class="row" style="margin-top:9px"><button class="smallbtn secondary" onclick="evaluateExperiment('${esc(id)}','${esc(accepted?.observation_id||'')}')" ${accepted?'':'disabled'}>Đánh giá snapshot đã được owner chấp nhận</button></div></div>`}catch(e){setStatus(e.message,'error')}}
async function applyMetaMapping(id,encodedVariants){const variants=JSON.parse(decodeURIComponent(encodedVariants));const campaignId=prompt('Nhập Meta campaign ID dạng số. Chỉ lấy từ Ads Manager/API, không suy đoán theo tên:');if(!campaignId)return;const mapping={};for(const variant of variants){const adId=prompt('Nhập Meta ad ID dạng số cho '+variant+':');if(!adId)return;mapping[variant]=adId}if(!confirm('XÁC NHẬN MAPPING NỘI BỘ?\n\nCampaign '+campaignId+' · '+variants.length+' variants. Việc này không sửa quảng cáo live và sẽ hủy preview cũ.'))return;try{const r=await api('/api/v1/experiments/'+encodeURIComponent(id)+'/tracking-mapping',{method:'POST',body:JSON.stringify({meta_ads_campaign_id:campaignId,variant_meta_ad_ids:mapping,note:'Owner verified IDs against Meta Ads Manager/API.'})});setStatus('Meta tracking '+r.state+'; chưa có thay đổi Ads live.','ok');await loadExperiments();await inspectExperimentEvidence(id)}catch(e){setStatus(e.message,'error')}}
function isoDay(d){return d.toISOString().slice(0,10)}
async function readExperimentSource(id,source){const end=new Date(),start=new Date();start.setUTCDate(start.getUTCDate()-13);try{setStatus('Đang kiểm tra tracking và đọc '+source+' ở chế độ chỉ-đọc…');const r=await api('/api/v1/experiments/'+encodeURIComponent(id)+'/source-read',{method:'POST',body:JSON.stringify({source_system:source,window_start:isoDay(start),window_end:isoDay(end)})});setStatus(r.message,r.state==='observed'?'ok':r.state==='no_data'?'':'error');await loadExperiments();await inspectExperimentEvidence(id)}catch(e){setStatus(e.message,'error')}}
async function decideObservationQuality(id,observationId,accepted){if(!confirm((accepted?'CHẤP NHẬN':'TỪ CHỐI')+' chất lượng snapshot?\n\nQuyết định chỉ mở khóa evaluation, không thực thi thay đổi bên ngoài.'))return;try{await api('/api/v1/experiments/'+encodeURIComponent(id)+'/observations/'+encodeURIComponent(observationId)+'/quality-decision',{method:'POST',body:JSON.stringify({accepted,note:accepted?'Owner xác nhận tracking, kỳ đo và aggregate variant hợp lệ.':'Owner từ chối snapshot do chất lượng/tracking chưa đạt.'})});await loadExperiments();await inspectExperimentEvidence(id)}catch(e){setStatus(e.message,'error')}}
async function evaluateExperiment(id,observationId=''){try{const e=await api('/api/v1/experiments/'+encodeURIComponent(id)+'/evaluations',{method:'POST',body:JSON.stringify({observation_id:observationId||null,min_sample_per_variant:100,max_source_age_hours:72,confidence_level:.95})});setStatus('Khuyến nghị '+e.recommendation+' đã được ghi audit; không có side effect.','ok');await loadExperiments();await inspectExperimentEvidence(id)}catch(e){setStatus(e.message,'error')}}
function render(s){const recent=(s.tasks||[]).slice(0,5);$('mTasks').textContent=(s.tasks||[]).length;$('mPending').textContent=s.approvals_pending;$('mExecuted').textContent=(s.tasks||[]).reduce((n,t)=>n+(t.executed_actions||0),0);$('mFailed').textContent=s.execution_failures;$('tasks').innerHTML=recent.length?recent.map(t=>`<tr><td>${dateTime(t.updated_at)}<div class="task-id">${esc(t.task_id)}</div></td><td class="task-objective">${esc(t.objective)}</td><td>${(t.selected_agents||[]).map(a=>`<span class="tag">${esc(a)}</span>`).join('')}</td><td>${number(t.total_actions)}</td><td>${number(t.approvals_pending)}</td><td>${number(t.failed_actions)}</td><td><div class="row"><button class="smallbtn" onclick="showAnswer('${esc(t.task_id)}')">Xem</button><button class="smallbtn secondary" onclick="reanalyze('${esc(t.task_id)}')">Phân tích lại</button></div></td></tr>`).join(''):'<tr><td colspan="7" class="muted">Chưa có task.</td></tr>';$('audit').innerHTML=(s.recent_audit||[]).slice(0,20).map(a=>`<tr><td>${dateTime(a.created_at)}</td><td>${esc(a.event_type)}</td><td><code>${esc(a.task_id)}</code></td><td>${esc(a.action_id||'')}</td><td>${esc(a.agent||'')}</td></tr>`).join('');loadApprovals(s.tasks||[])}
function metricValue(label,value){const key=String(label).toLocaleLowerCase('vi');if(key.includes('chi phí')||key.includes('đã chi')||key.includes('cpc')||key.includes('cpl'))return money(value);if(typeof value==='number')return number(value,Number.isInteger(value)?0:2);return esc(value)}
function detailValue(details,keys){for(const key of keys)if(details[key]!==undefined&&details[key]!==null)return details[key];return null}
function campaignItems(items){return items.length>0&&items.every(i=>String(i.title||'').startsWith('Chiến dịch:'))}
function renderCampaignTable(items){const rows=items.map(i=>{const d=i.details||{};const spend=detailValue(d,['Số tiền đã chi (VND)','Số tiền đã chi','Đã chi (VND)','Chi phí']);const impressions=Number(detailValue(d,['Impressions']))||0;const clicks=Number(detailValue(d,['Clicks']))||0;const leads=Number(detailValue(d,['Lead Meta','Lead do Meta báo cáo']))||0;const ctr=detailValue(d,['CTR (%)'])??(impressions?clicks*100/impressions:0);const cpl=detailValue(d,['CPL Meta (VND)','CPL Meta'])??(leads?Number(spend||0)/leads:null);return `<tr><td><strong>${esc(String(i.title).replace(/^Chiến dịch:\s*/,''))}</strong><div class="task-id">${esc(detailValue(d,['Tài khoản Ads'])||i.entity_id||'')}</div></td><td><span class="priority ${esc(i.priority)}">${i.priority==='high'?'Chi tiêu cao':esc(i.priority)}</span></td><td>${money(spend)}</td><td>${number(impressions)}</td><td>${number(clicks)}</td><td>${number(ctr,2)}%</td><td>${number(leads)}</td><td>${cpl===null?'—':money(cpl)}</td><td>${esc(i.recommended_action)}</td></tr>`}).join('');return `<div class="campaign-wrap"><h4>Bảng so sánh chiến dịch</h4><div class="panel"><table class="campaign-table"><thead><tr><th>Chiến dịch / Tài khoản</th><th>Mức chú ý</th><th>Số tiền đã chi (VND)</th><th>Impressions</th><th>Clicks</th><th>CTR</th><th>Lead Meta</th><th>CPL Meta</th><th>Đề xuất</th></tr></thead><tbody>${rows}</tbody></table></div></div>`}
function renderGenericItems(items){return items.map(i=>`<div class="answer-item ${esc(i.priority)}"><strong>${esc(i.title)}</strong> · ${esc(i.priority)}<div>${esc(i.reason)}</div><div class="muted">${Object.entries(i.details||{}).map(([k,v])=>`${esc(k)}: ${esc(v??'—')}`).join(' · ')}</div><div><strong>Đề xuất:</strong> ${esc(i.recommended_action)}</div></div>`).join('')}
function renderAnswer(r){const a=r&&r.answer;if(!a){$('answer').className='notice';$('answer').innerHTML='Nhiệm vụ chưa có kết luận từ dữ liệu thật.';return}const metrics=Object.entries(a.metrics||{}).map(([k,v])=>`<div class="answer-metric"><strong>${esc(k)}</strong><span class="value">${metricValue(k,v)}</span></div>`).join('');const answerItems=a.items||[];const items=campaignItems(answerItems)?renderCampaignTable(answerItems):renderGenericItems(answerItems);const rec=(a.recommendations||[]).map(x=>`<li>${esc(x)}</li>`).join('');const caveats=(a.caveats||[]).map(x=>`<li>${esc(x)}</li>`).join('');const evidence=(a.evidence||[]).map(x=>`<li>${esc(x)}</li>`).join('');$('answer').className='card answer';$('answer').innerHTML=`<div class="row" style="justify-content:space-between"><h3>${esc(a.title)}</h3><span class="tag">${esc(a.status)}</span></div><p>${esc(a.summary)}</p><div class="answer-metrics">${metrics}</div>${items||'<div class="muted">Không có mục nghiệp vụ cần liệt kê.</div>'}${rec?`<h4>Việc nên làm</h4><ul>${rec}</ul>`:''}${evidence?`<h4>Bằng chứng</h4><ul>${evidence}</ul>`:''}${caveats?`<h4>Giới hạn cần biết</h4><ul>${caveats}</ul>`:''}`}
async function showAnswer(task){try{const r=await api('/api/v1/agent-tasks/'+encodeURIComponent(task));renderAnswer(r);$('answer').scrollIntoView({behavior:'smooth',block:'start'})}catch(e){setStatus(e.message,'error')}}
async function reanalyze(task){try{setStatus('Đang đọc lại dữ liệu và phân tích…');const r=await api('/api/v1/agent-tasks/'+encodeURIComponent(task)+'/analyze',{method:'POST'});renderAnswer(r);setStatus('Đã cập nhật kết quả '+r.task_id,'ok');await refreshAll()}catch(e){setStatus(e.message,'error')}}
const APPROVAL_EXECUTION_DETAILS={
'crm.records.update':{target:'EspoCRM',effect:'Khi có lệnh thực thi riêng, hệ thống gửi yêu cầu qua n8n write executor để cập nhật tag, stage hoặc field của các bản ghi CRM đã chỉ định.',required:[{keys:['record_ids','records'],label:'ID bản ghi CRM'},{keys:['changes','field_updates'],label:'field, tag hoặc stage và giá trị trước–sau'}]},
'ads.budget.update':{target:'Meta Ads',effect:'Khi có lệnh thực thi riêng, hệ thống gửi yêu cầu thay đổi ngân sách cho campaign hoặc ad set đã chỉ định.',required:[{keys:['account_id'],label:'tài khoản quảng cáo'},{keys:['campaign_id','adset_id'],label:'campaign hoặc ad set'},{keys:['new_budget','budget'],label:'ngân sách mới'},{keys:['currency'],label:'đơn vị tiền tệ'}]},
'social.publish':{target:'Kênh Social',effect:'Khi có lệnh thực thi riêng, hệ thống đăng nội dung đã duyệt lên Page hoặc kênh và lịch đã chỉ định.',required:[{keys:['page_id','channel'],label:'Page hoặc kênh đích'},{keys:['post_id','content_id','media_ids'],label:'nội dung và media cuối cùng'},{keys:['scheduled_at','publish_at'],label:'thời điểm đăng'}]},
'sales.contact.send':{target:'Kênh liên hệ khách hàng',effect:'Khi có lệnh thực thi riêng, hệ thống gửi nội dung đã duyệt cho lead qua kênh đã chỉ định.',required:[{keys:['lead_ids','contact_ids'],label:'lead hoặc người nhận'},{keys:['channel'],label:'kênh liên hệ'},{keys:['template_id','message_id'],label:'mẫu hoặc nội dung cuối cùng'},{keys:['scheduled_at','send_at'],label:'thời điểm gửi'}]}
};
const SAFE_APPROVAL_SCOPE=[['account_id','Tài khoản'],['campaign_id','Campaign'],['adset_id','Ad set'],['page_id','Page'],['channel','Kênh'],['currency','Tiền tệ'],['new_budget','Ngân sách mới'],['budget','Ngân sách'],['scheduled_at','Lịch thực hiện'],['publish_at','Lịch đăng'],['send_at','Lịch gửi']];
function approvalPayload(action){return action.payload&&typeof action.payload==='object'&&!Array.isArray(action.payload)?action.payload:{}}
function hasApprovalValue(payload,keys){return keys.some(key=>{const v=payload[key];return Array.isArray(v)?v.length>0:(v&&typeof v==='object'?Object.keys(v).length>0:v!==undefined&&v!==null&&v!=='')})}
function renderApprovalScope(action){const payload=approvalPayload(action);const parts=[];for(const [key,label] of SAFE_APPROVAL_SCOPE){if(!hasApprovalValue(payload,[key]))continue;const value=(key==='new_budget'||key==='budget')?money(payload[key],payload.currency||'VND'):payload[key];parts.push(`${esc(label)}: <strong>${esc(value)}</strong>`)}for(const [key,label] of [['record_ids','Bản ghi CRM'],['lead_ids','Lead'],['contact_ids','Người nhận'],['media_ids','Media']]){if(Array.isArray(payload[key])&&payload[key].length)parts.push(`${esc(label)}: <strong>${number(payload[key].length)}</strong>`)}if(payload.changes&&typeof payload.changes==='object')parts.push(`Trường thay đổi: <strong>${number(Object.keys(payload.changes).length)}</strong>`);return parts.length?parts.join(' · '):'Chưa có ID đối tượng hoặc giá trị thay đổi cụ thể trong action.'}
function renderApprovalExecution(action){const spec=APPROVAL_EXECUTION_DETAILS[action.tool]||{target:'Hệ thống bên ngoài',effect:'Khi có lệnh thực thi riêng, hệ thống mới gửi action này tới executor được cấu hình.',required:[{keys:[],label:'đối tượng, dữ liệu thay đổi và thời điểm thực hiện'}]};const payload=approvalPayload(action);const missing=spec.required.filter(item=>!hasApprovalValue(payload,item.keys)).map(item=>item.label);return `<div class="approval-plan"><h4>Sau khi được phê duyệt</h4><ol><li>Action chuyển sang trạng thái <strong>approved</strong>, rời Approval queue và được ghi vào audit.</li><li><strong>Chưa tự ghi dữ liệu hoặc liên hệ bên ngoài.</strong> Cần một lệnh thực thi riêng của Operator/Commander.</li><li>${esc(spec.effect)}</li></ol><div class="approval-facts"><div class="approval-fact"><span>Hệ thống đích</span><strong>${esc(spec.target)}</strong></div><div class="approval-fact"><span>Mã công cụ</span><strong>${esc(action.tool||'—')}</strong></div></div><p class="approval-reason"><strong>Vì sao cần duyệt:</strong> ${esc(action.approval_reason||'Action có tác động ghi hoặc tác động bên ngoài.')}</p><div class="approval-scope"><strong>Phạm vi hiện có:</strong> ${renderApprovalScope(action)}</div>${missing.length?`<div class="approval-warning"><strong>Còn thiếu trước khi thực thi:</strong> ${missing.map(esc).join('; ')}. Owner nên yêu cầu bổ sung các thông tin này trước khi cho chạy thật.</div>`:''}</div>`}
async function loadApprovals(tasks){const out=[];for(const t of tasks.slice(0,20)){try{const r=await api('/api/v1/agent-tasks/'+encodeURIComponent(t.task_id));for(const a of (r.approvals_required||[]))out.push({task:t,action:a})}catch{}}$('approvals').innerHTML=out.length?out.map(x=>`<div class="card approval"><div class="approval-meta"><span class="tag">${esc(x.action.agent)}</span><span class="priority ${esc(x.action.risk)}">risk: ${esc(x.action.risk)}</span></div><strong>${esc(x.action.title)}</strong><p>${esc(x.action.description)}</p><div class="muted">Task: ${esc(x.task.objective)}</div>${renderApprovalExecution(x.action)}<div class="row" style="margin-top:10px"><button class="smallbtn" onclick="decision('${esc(x.task.task_id)}','${esc(x.action.action_id)}',true)">Phê duyệt</button><button class="smallbtn danger" onclick="decision('${esc(x.task.task_id)}','${esc(x.action.action_id)}',false)">Từ chối</button></div></div>`).join(''):'<div class="notice empty">Không có approval đang chờ.</div>'}
async function decision(task,action,approved){const verb=approved?'PHÊ DUYỆT':'TỪ CHỐI';const approvalNote=approved?'\n\nPhê duyệt chỉ mở khóa action và ghi audit; không tự ghi dữ liệu. Hành động bên ngoài chỉ xảy ra khi có lệnh thực thi riêng.':'';if(!confirm(`${verb} action này?\n\nHãy chắc chắn bạn đã kiểm tra dữ liệu, phạm vi và rủi ro.${approvalNote}`))return;try{await api(`/api/v1/agent-tasks/${encodeURIComponent(task)}/actions/${encodeURIComponent(action)}/decision`,{method:'POST',body:JSON.stringify({approved})});await refreshAll()}catch(e){setStatus(e.message,'error')}}
async function createTask(){const objective=$('objective').value.trim();if(objective.length<3)return setStatus('Mục tiêu quá ngắn','error');try{setStatus('AI Agents đang đọc dữ liệu và phân tích yêu cầu…');const r=await api('/api/v1/agent-tasks',{method:'POST',body:JSON.stringify({objective})});renderAnswer(r);setStatus('Đã có kết quả cho task '+r.task_id,'ok');await refreshAll()}catch(e){setStatus(e.message,'error')}}
renderQuestions();if(token)refreshAll();
</script>
</body></html>'''


def command_center_html(*, browser_login_enabled: bool = False) -> HTMLResponse:
    content = DASHBOARD_HTML
    if browser_login_enabled:
        content = content.replace(
            '<div class="auth"><input id="token" type="password" autocomplete="off" placeholder="Bearer token (owner/operator/viewer)"/><button onclick="saveToken()">Kết nối</button></div>',
            '<div class="auth"><span id="identity">Đang xác thực…</span><a class="secondary" href="/logout">Đăng xuất</a></div>',
        ).replace(
            'Nhập token để tải dữ liệu. Token chỉ lưu trong sessionStorage của trình duyệt.',
            'Đang tải dữ liệu Command Center…',
        ).replace(
            "const $=id=>document.getElementById(id);let token=sessionStorage.getItem('npd_agent_token')||'';$('token').value=token;let activeQuestionGroup='Tất cả';",
            "const $=id=>document.getElementById(id);let token='';let activeQuestionGroup='Tất cả';",
        ).replace(
            "function saveToken(){token=$('token').value.trim();sessionStorage.setItem('npd_agent_token',token);refreshAll()}",
            "function saveToken(){}",
        ).replace(
            "if(!r.ok){let d;try{d=await r.json()}catch{d={detail:r.statusText}};throw new Error((d&&d.detail)||('HTTP '+r.status))}",
            "if(r.status===401){location.href='/login';throw new Error('login required')}if(!r.ok){let d;try{d=await r.json()}catch{d={detail:r.statusText}};throw new Error((d&&d.detail)||('HTTP '+r.status))}",
        ).replace(
            "setStatus('Đã xác thực: '+me.role+' · storage: '+s.storage_backend,'ok');render(s);renderSources(sources)",
            "$('identity').textContent=me.subject+' · '+me.role;setStatus('Đã xác thực: '+me.subject+' · '+me.role+' · storage: '+s.storage_backend,'ok');render(s);renderSources(sources)",
        ).replace("renderQuestions();if(token)refreshAll();", "renderQuestions();refreshAll();")
    return HTMLResponse(
        content,
        headers={
            "Cache-Control": "no-store",
            "Content-Security-Policy": "default-src 'self'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'",
        },
    )
