[project]
name = "npd-video-worker"
version = "0.1.0"
description = "Pipeline worker for NPD AI Video Factory"
requires-python = ">=3.12"
dependencies = [
  "pydantic-settings>=2.5,<3",
  "redis>=5,<7",
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
